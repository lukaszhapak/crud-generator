package $packageName;

import $packageName.entity.$entityName;
import $packageName.repository.$entityNameRepository;
import org.springframework.stereotype.Service;
import lombok.RequiredArgsConstructor;

import java.util.List;

@RequiredArgsConstructor
@Service
public class $entityNameService {

    private final $entityNameRepository $entityLowerCaseRepository;


    public List<$entityName> findAll() {
        return $entityLowerCaseRepository.findAll();
    }

    public void save($entityName $entityLowerCase) {
        $entityLowerCaseRepository.save($entityLowerCase);
    }

    public void update($entityName $entityLowerCase) {
        $entityLowerCaseRepository.save($entityLowerCase);
    }

    public $entityName findById(String id) {
        return $entityLowerCaseRepository.findById(Long.parseLong(id)).orElse(null);
    }

    public void deleteById(String id) {
        $entityLowerCaseRepository.deleteById(Long.parseLong(id));
    }
}
